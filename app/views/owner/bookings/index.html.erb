<div class='text-center'> <h1>Mes réservations</h1> </div>
<div class="container">
  <div class="row" >
    <% @bookings.each do |booking| %>
      <div class="col-4">
        <div class="card-trip">
          <img src="https://images.unsplash.com/photo-1486092642310-0c4e84309adb?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=800&q=60" />
            <div class="card-trip-infos">
              <div>
                <h2><%= booking.instrument.description %></h2>
                <%#= link_to instrument.description, owner_instrument_path(instrument), class: 'btn btn-secondary' %>
                <p><%= booking.begin_at %> <%= booking.begin_at %> <%= booking.final_price %></p>
              </div>
              <%= link_to "delete", owner_booking_path(booking.id), method: :delete, class: "btn btn-warning"%>
            </div>
          </div>
        </div>
    <% end %>
  </div>
</div>
