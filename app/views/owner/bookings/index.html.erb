<div class='text-center'> <h1>Mes réservations</h1> </div>
<div class="container">
  <div class="row" >
    <% @bookings.each do |booking| %>
      <div class="col-4">
        <div class="card-trip">
          <%= cl_image_tag booking.instrument.photo, height: 200, width: 300, crop: :fill %>
            <div class="card-trip-infos">
              <div>
                <h2><%= booking.instrument.description %></h2>
                <p><%= booking.begin_at %> <%= booking.begin_at %> <%= booking.final_price %></p>
              </div>
              <%= link_to "Del", owner_booking_path(booking), method: :delete, class: "btn btn-warning"%>
            </div>
          </div>
        </div>
    <% end %>
  </div>
</div>
