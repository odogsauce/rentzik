   <div class="container">
    <%= simple_form_for :instrument, url: owner_instruments_path, method: :post do |f| %>
    <%= f.input :description %>
    <%= f.input :category %>
    <%= f.input :unit_price %>
    <%= f.submit "Save instrument", class: "btn btn-success" %>
    <% end %>
   </div>
